# Database Configuration
MONGODB_URI=mongodb://localhost:27017/rashtram_ai

# JWT Secret Key (CRITICAL: Change this to a strong, unique secret in production)
# Generate a secure secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-key-here-change-this-in-production-64-chars-long

# Client Configuration
CLIENT_URL=http://localhost:3000

# Environment
NODE_ENV=development

# Server Configuration
PORT=5001
